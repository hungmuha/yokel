Yokel is an app that connects outsiders to the locals, so they can find out about all the "hotspots" . The app will be mainly used on mobile.

The technology involed:
	.SQL
	.Express
	.Angular
	.Node
	.Geolocation
	.outside API (google map, weather)
	.animation with CSS
